#include <stdio.h>

void QuickSort(int *a, int low, int high)
{
    int
}

int FindPos(int *a, int low, int high)
{
    int val = a[low];

    while(low < high)
    {

    }
}

int main(void)
{
    int a[6] = {4, 2, 6, 7, 9, 1};
}
